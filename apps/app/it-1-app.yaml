apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: deploy-app-it-1
  namespace: openshift-gitops
spec:
  project: default
  source:
    repoURL: 'https://github.com/kforetas/petclinic-gitops'
    targetRevision: HEAD
    path: manifest/app/overlays/it-1
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: petclinic-it-1
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
